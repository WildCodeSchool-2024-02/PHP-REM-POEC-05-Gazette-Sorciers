
    <h1>Categories</h1>
    <ul>
        {% for category in categories %}
            <li><a href="/categories/show?id={{ category.id_category }}">{{ category.id_category }}. {{ category.name }}</a></li>
        {% else %}
            <li>Nothing to display</li>
        {% endfor %}
    </ul>
    <a href="/categories/add">Ajouter</a>